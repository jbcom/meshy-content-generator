# meshy-content-generator

Standalone Python package for declarative Meshy image-generation pipelines.
Read `AGENTS.md` and `docs/architecture.md` first. Meshy transport is supplied
by `vendor-fabric`; this repository must never grow another HTTP client.

